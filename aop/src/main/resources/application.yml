spring:
  application:
    name: aop
  datasource:
    url: jdbc:h2:file:/data/aop_practice_db
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update